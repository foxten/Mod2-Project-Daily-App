<h2 id="welcome">Book Library</h2>

<div id="body3">
<% @books.each do |book| %>
    <li><%= link_to book.title, book_path(book) %></li>
<% end %>

<br><%= button_to "Add Book", new_book_path, method: :get %>
</div>